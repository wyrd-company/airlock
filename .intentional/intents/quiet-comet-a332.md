---
airlock: minor
---

Add deterministic working-tree alignment. Airlock authors only deterministic-file remediation findings, reports every path operation and dirty-tree skip, re-observes the local result, and exposes read-only pull-request context while leaving staging, commits, pushes, and draft pull requests to the caller. Judgment-file findings are handed to an agent with the embedded repository-standards skill, and operator settings remain human-only.
